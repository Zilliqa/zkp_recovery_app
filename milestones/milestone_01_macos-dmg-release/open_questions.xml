<open-questions>
  <open-question id="mopro build handling">
    <question>Should the script always run mopro build (which regenerates the committed Dart bindings and can leave the working tree dirty), make it optional with a flag, or skip it by default? If mopro-cli is missing, should the script just fail with install instructions or install it automatically?</question>
    <alternative id="Always run, fail if missing">
      The script always runs `mopro build` before `flutter build macos`. If `mopro` is not on PATH, it stops straight away and prints the install command (`cargo install mopro-cli`). If the committed bindings under `mopro_flutter_bindings/lib/src/rust/` change, it reports that with a `git status`/`git diff --stat` notice after the step.
      <advantage>This matches the milestone Goal as written (&quot;runs `mopro build` and `flutter build macos`&quot;). Every dmg is built from bindings freshly regenerated from the current Rust API and circuit, so a stale-bindings release can&apos;t happen. The script also never changes the machine&apos;s toolchain without the maintainer knowing.</advantage>
      <drawback>Every run needs mopro-cli, which is not installed on the developer machine today. `mopro build` also adds a full release Rust compile on top of the one cargokit already does during `flutter build macos`. A regeneration can leave committed generated files dirty, for example from a different mopro-cli or flutter_rust_bridge version than the one that produced the frb 2.11.1 bindings. Whether that is only reported or blocks the build depends on how the clean-working-tree prerequisite is settled.</drawback>
    </alternative>
    <alternative id="Always run, auto-install mopro">
      The script always runs `mopro build`. If `mopro` is missing, it installs mopro-cli itself with `cargo install mopro-cli`, ideally pinned with `--version`/`--locked` to a release compatible with the crate&apos;s `mopro-ffi =0.3.7`, and then continues.
      <advantage>A new maintainer machine goes from checkout to dmg with one command and no manual setup. That would have covered the one gap found on this machine, where mopro was missing.</advantage>
      <drawback>The build script changes the maintainer&apos;s global cargo install, and a first run takes a long time. Unless the version is pinned and kept current by hand, `cargo install mopro-cli` picks up the latest CLI, which may generate bindings that don&apos;t match the committed ones or the pinned `mopro-ffi =0.3.7`/frb 2.11.1. That puts an unreviewed toolchain change into the release artifact.</drawback>
    </alternative>
    <alternative id="Run by default, skip flag">
      `mopro build` runs by default. An opt-out flag (e.g. `--skip-mopro`) builds straight from the committed bindings. mopro-cli is only required, and a missing one only fails the run with install instructions, when the step isn&apos;t skipped.
      <advantage>The Goal&apos;s default behaviour and its freshness guarantee stay the same. Maintainers who know the Rust API and circuit haven&apos;t changed can skip the extra Rust compile and the dirty-tree churn, and a machine without mopro can still build.</advantage>
      <drawback>Two build paths need documenting and testing. Also, the skip is chosen by the maintainer rather than detected, so a release can still ship with stale bindings if someone uses the flag after a Rust API change.</drawback>
    </alternative>
    <alternative id="Skip by default, opt-in flag">
      By default the script builds from the committed Dart bindings and relies on cargokit&apos;s pod &quot;Build Rust library&quot; phase to compile the native library during `flutter build macos`. `mopro build` runs only with an opt-in flag (e.g. `--regen-bindings`), and mopro-cli is checked (fail with instructions) only in that case.
      <advantage>The release build treats the committed bindings as the source of truth and never dirties the working tree. It doesn&apos;t need mopro-cli at all, and it skips a redundant release Rust compile, since the native library is built by cargokit either way. Regenerating bindings stays a deliberate step that gets committed, not a side effect of packaging.</advantage>
      <drawback>This goes against the Goal as written, which lists `mopro build` as a step the script runs, so choosing it means amending the Goal. It also relies on the committed bindings actually matching the Rust API. If someone changes the Rust API or circuit and forgets to run `mopro build` and commit, the mismatch only shows up as a build or runtime failure instead of being regenerated automatically.</drawback>
    </alternative>
    <recommendation option="Always run, fail if missing">It matches the goal as written with a single code path, guarantees every dmg ships freshly regenerated bindings, never alters the maintainer&apos;s toolchain, and repeated dry runs stay cheap because cargo&apos;s target/ cache makes the second mopro build incremental.</recommendation>
  </open-question>
  <open-question id="Clean build policy">
    <question>Should the script always start from a clean build (e.g. flutter clean and removing the previous Release products) so release artifacts are reproducible? Or should it reuse incremental build state for speed, perhaps with a flag to choose?</question>
    <alternative id="Always clean">
      The script always starts with `flutter clean` and deletes any earlier Release products and staging output. No incremental state carries over into a release dmg.
      <advantage>Every dmg is built from the checked-out sources and nothing else. A leftover Debug build (the only thing now under `flutter/build/macos/Build/Products/`), stale pods, or a stale cargokit Rust library can&apos;t slip into a release, so the published SHA-256 always comes from the same kind of build.</advantage>
      <drawback>Every run is slow. `flutter/build` is about 2.2 GB, and cargokit&apos;s Rust output sits in Xcode&apos;s `TARGET_TEMP_DIR` inside it. Cleaning therefore forces a full recompile of the Arkworks/circom-prover crate, including the `rust_witness` transpile of the ~679k-constraint `groth.wasm`, on every attempt. That hurts most while the milestone&apos;s dry run is being fixed. Also, a clean build is not byte-reproducible anyway, because timestamps and ad-hoc signatures still vary.</drawback>
    </alternative>
    <alternative id="Incremental by default, --clean flag">
      By default the script reuses the existing Flutter, pod and cargokit build state. A `--clean` flag runs `flutter clean` and removes the earlier Release products before building.
      <advantage>Quick iteration while developing and debugging the script, since repeated runs skip the long Rust rebuild. A clean release build is still one flag away.</advantage>
      <drawback>A maintainer can forget the flag, and the default path is the less trustworthy one. A dmg built on stale state, such as bindings or a Rust library from before a `mopro build` or version bump, looks just like a clean one. Nothing records whether a published artifact was built clean.</drawback>
    </alternative>
    <alternative id="Clean by default, --incremental flag">
      By default the script cleans the Flutter build and earlier Release products. An opt-in flag such as `--incremental` or `--no-clean` skips the clean so a developer can iterate quickly.
      <advantage>The default is the safe one for release artifacts, and a fast path is still there for script development and repeated dry runs. The warning could mark incremental builds, for example in output or dmg naming, so they aren&apos;t mistaken for release builds.</advantage>
      <drawback>There are two code paths to maintain and document. Anyone who habitually passes the fast flag loses the guarantee without noticing, unless the script clearly labels incremental output as not for release.</drawback>
    </alternative>
    <alternative id="Targeted clean only">
      The script never runs a full `flutter clean`. Instead it always deletes only the outputs that decide what gets shipped: the Release `.app` under `flutter/build/macos/Build/Products/Release/` and the dmg staging directory. Xcode, pod and cargokit compile caches are left for Flutter and Xcode&apos;s own dependency tracking to invalidate.
      <advantage>Most of the benefit at little cost. A stale or leftover app bundle can never be packaged, because the bundle that gets signed and put in the dmg is always freshly produced by this run&apos;s `flutter build macos`. The expensive Rust compile cache is kept.</advantage>
      <drawback>It relies on Xcode and cargokit invalidating their caches correctly, for example after the bindings are regenerated or the pubspec version changes. Any mistake in that dependency tracking ends up in the release unnoticed. It also differs from a truly clean build if the machine&apos;s toolchain or pods change between runs.</drawback>
    </alternative>
    <recommendation option="Targeted clean only">Deleting the Release .app and staging directory every run guarantees the packaged bundle is always produced by this run&apos;s flutter build, with one code path and no flag, while keeping the expensive cargokit Rust cache that a full flutter clean would throw away on every dry-run attempt.</recommendation>
  </open-question>
  <open-question id="Checksum publication format">
    <question>Should the script only print the dmg&apos;s SHA-256, or also write it to a sidecar file (e.g. &lt;dmg&gt;.sha256 in shasum format) that can be attached to the GitHub release? Which command should the user doc tell users to run to check it (shasum -a 256)?</question>
    <alternative id="Print only, compare by eye">
      The script prints the dmg&apos;s SHA-256 to the terminal and writes no file. The maintainer pastes the hash into the GitHub release notes. docs/macOS.md tells users to run `shasum -a 256 &lt;dmg&gt;` and compare the output by eye with the hash on the release page. This matches the flow in docs/Linux.md (`sha256sum` on the file, then compare with the hash listed on the official release page).
      <advantage>This is the smallest change and matches both the Goal text (&quot;the script prints its SHA-256&quot;) and the existing Linux guide. There is no extra artifact to upload or keep in sync, and the macOS and Linux user instructions stay parallel.</advantage>
      <drawback>The maintainer has to copy the hash into the release notes by hand, so a paste error or a stale hash can slip through. Users also have to compare 64 hex characters by eye, which is easy to get wrong and gives no clear pass/fail result.</drawback>
    </alternative>
    <alternative id="Print plus shasum sidecar, compare by eye">
      The script prints the SHA-256 and also writes `&lt;dmg&gt;.sha256` next to the dmg, in the standard `shasum -a 256` output format (`&lt;hash&gt; &lt;filename&gt;`, filename only, no directory). The maintainer attaches that file to the GitHub release. docs/macOS.md still tells users to run `shasum -a 256 &lt;dmg&gt;` and compare the result with the published hash (from the release page or the .sha256 asset).
      <advantage>The release gets a checksum file that machines can read, and it comes straight from the build, so no copying by hand is involved. The user doc stays as simple as the Linux guide, and users who know `shasum -c` can use it.</advantage>
      <drawback>There is one more artifact for the maintainer to upload, and it must not go stale if the dmg is rebuilt. The sidecar also only helps with transport errors: it is published by the same hand from the same place as the dmg, so anyone who can swap the dmg can swap the .sha256 too. And unless the doc shows `shasum -c`, users still compare by eye.</drawback>
    </alternative>
    <alternative id="Print plus sidecar, doc uses shasum -c">
      The script prints the SHA-256 and writes the `&lt;dmg&gt;.sha256` sidecar in shasum format, with the bare filename so it checks correctly from ~/Downloads. docs/macOS.md tells users to download both files into the same directory and run `shasum -a 256 -c &lt;dmg&gt;.sha256`, which prints `&lt;dmg&gt;: OK` or FAILED. The printed hash is still shown for anyone who wants to compare by eye.
      <advantage>Users get a clear OK/FAILED result with no manual hex comparison, which makes the &quot;DO NOT PROCEED if it doesn&apos;t match&quot; instruction easy to follow. The file format and the verification command are designed as a pair.</advantage>
      <drawback>Users have two downloads to manage, and the check fails confusingly if the sidecar&apos;s filename doesn&apos;t exactly match the downloaded dmg&apos;s name (for example, if the browser renamed it to &quot;…(1).dmg&quot;). The steps also differ from docs/Linux.md. And, as with any sidecar, it gives no protection against tampering at the release source.</drawback>
    </alternative>
    <recommendation option="Print plus shasum sidecar, compare by eye">Writing the .sha256 next to the dmg costs one line, removes the hand-copied hash from the release process, and keeps docs/macOS.md parallel to the Linux guide&apos;s compare-by-eye step without the renamed-download pitfalls of shasum -c.</recommendation>
  </open-question>
  <open-question id="Gatekeeper bypass instructions">
    <question>Which Gatekeeper workaround(s) should docs/macOS.md document for the unsigned app, given the minimum supported macOS is 12 and recent macOS versions dropped the right-click → Open shortcut? Options: System Settings → Privacy &amp; Security → Open Anyway, xattr -dr com.apple.quarantine on the installed app, or both. Should the doc explain the &quot;damaged&quot;/&quot;unidentified developer&quot; messages users will see?</question>
    <alternative id="Open Anyway only">
      Document only the GUI route in docs/macOS.md: try to launch the app once, then go to System Settings → Privacy &amp; Security (System Preferences → Security &amp; Privacy on macOS 12) and click &quot;Open Anyway&quot;. Include a brief note on the blocking dialog so users recognise it.
      <advantage>It is the one route Apple supports and it works on every macOS version the app targets, from 12 up to the current 26. It needs no Terminal, and it clears the block only for this one app instead of teaching users to strip security metadata. For a crypto-recovery app whose users must trust what they run, that is the most conservative stance.</advantage>
      <drawback>It takes several steps and the panel&apos;s wording and location differ between macOS 12 (System Preferences) and 13+ (System Settings). The button only shows for about an hour after a blocked launch, so users get stuck easily. It also does not help if the app is reported as &quot;damaged&quot;: that happens when the signature is missing or broken, which can occur depending on how re-signing and entitlements are settled. When the app is damaged, macOS offers no Open Anyway button at all.</drawback>
    </alternative>
    <alternative id="xattr only">
      Document only the Terminal route: after dragging the app to /Applications, run `xattr -dr com.apple.quarantine &quot;/Applications/Zero Knowledge Migration App.app&quot;` (quoted, because the bundle name has spaces). Then launch the app normally.
      <advantage>A single copy-paste command works the same way on macOS 12 through 26. It also clears the &quot;damaged&quot; case as well as the &quot;unidentified developer&quot; / &quot;cannot verify&quot; case, so one instruction covers every Gatekeeper outcome. It matches the command-line style of docs/Linux.md, where users already run sha256sum and a terminal launch.</advantage>
      <drawback>It asks non-technical users to open Terminal and run a command that turns off a security check, with no GUI fallback. Removing the quarantine flag recursively also removes the safety net for anything else in the bundle, which is a harder trust sell in a guide whose checksum step exists to build that trust. If the path is mistyped or unquoted, the command fails with a confusing error.</drawback>
    </alternative>
    <alternative id="Both, GUI first">
      Document Open Anyway as the main path and `xattr -dr com.apple.quarantine` on the installed app as a fallback. Add a short section on the messages users may see: &quot;Apple could not verify … is free of malware&quot; / &quot;unidentified developer&quot; (use Open Anyway) and &quot;… is damaged and can&apos;t be opened&quot; (use the xattr command). Also explain why they appear: the app is not signed with a Developer ID or notarized.
      <advantage>It covers every outcome the unsigned build can produce on macOS 12–26 while leading with the safer, supported route. Matching each message to its fix stops users from deleting the app or assuming it is malware when they see &quot;damaged&quot;. That reduces support load and failed claims.</advantage>
      <drawback>The Gatekeeper section is the longest of the options and asks users to choose a path, which is more to maintain as Apple changes the wording and layout of the dialogs and the settings pane. The &quot;damaged&quot; guidance may cover a case that never happens if the shipped signature proves valid. Only the manual launch test planned after the milestone can confirm which messages actually appear.</drawback>
    </alternative>
    <alternative id="Version-tiered steps">
      Give instructions per macOS version: right-click (Control-click) → Open on macOS 12–14, where that shortcut still works, and System Settings → Privacy &amp; Security → Open Anyway on macOS 15 and later. Keep `xattr -dr com.apple.quarantine` as a final fallback, and explain the &quot;unidentified developer&quot; / &quot;damaged&quot; messages.
      <advantage>Each user gets the shortest route their OS allows. On 12–14 that is the familiar right-click → Open, which is quicker than the Settings round-trip, so the guide is as precise as possible across the supported range.</advantage>
      <drawback>Users must first check which macOS version they have, and the guide ends up with three procedures that each need screenshots or wording kept up to date. The right-click branch helps only users of older versions and adds a branch point where they can pick the wrong path. Open Anyway already works on those versions, so the split adds complexity without making anything newly possible.</drawback>
    </alternative>
    <recommendation option="Both, GUI first">Leading with Open Anyway keeps the supported route for macOS 12 through 26, and the xattr fallback with a message-to-fix table covers the damaged case an ad-hoc signature can still produce, so users are not left stuck or assuming the app is malware.</recommendation>
  </open-question>
  <open-question id="Screenshot and doc links">
    <question>Should docs/macOS.md include a screenshot (docs/macos.png) like the Linux and Windows guides? Should any existing doc (e.g. the root README.md or flutter/README.md) link to the new macOS guide?</question>
    <alternative id="Screenshot, no new links">
      docs/macOS.md opens with an embedded screenshot (docs/macos.png) of the app running on macOS, as docs/Linux.md does with `![Linux desktop screenshot](linux.png)`, and no other doc is changed to link to the new guide.
      <advantage>It matches the only guide that actually embeds its screenshot, docs/Linux.md. docs/Windows.md has a windows.png beside it but never references it. Leaving the other docs alone follows the current convention, since nothing in the repo links to Linux.md or Windows.md either. The root README.md and flutter/README.md are stock mopro templates, and a project link would sit oddly in them.</advantage>
      <drawback>Taking the screenshot means launching a Release build. The goal says launch will be tested manually after the milestone, and the done criterion is only the build, sign and dmg dry run. So the image either becomes extra work inside the milestone or the guide ships with a broken image reference until someone captures it. The guide also stays hard to find, because nothing links to any of the platform guides.</drawback>
    </alternative>
    <alternative id="Screenshot plus README links">
      docs/macOS.md embeds docs/macos.png like docs/Linux.md does, and the root README.md gets a short platform-guides section that links docs/Linux.md, docs/Windows.md and the new docs/macOS.md.
      <advantage>Anyone landing on the repo can find the macOS guide, and the two existing guides that currently have no inbound links become findable too. The new guide also looks the same as the Linux one.</advantage>
      <drawback>It has the same problem getting the screenshot as the other screenshot option, because it needs a launched Release app outside the milestone&apos;s dry-run done criterion. It also edits the root README.md, which is the stock mopro template, not project documentation. That widens a macOS packaging milestone into general docs cleanup, and the link section sits next to template text that describes a different project.</drawback>
    </alternative>
    <alternative id="Text-only, no new links">
      docs/macOS.md has no screenshot and no image reference, and no existing doc is changed.
      <advantage>It is the smallest change and depends only on the build, sign and dmg dry run. Nothing in the guide waits for the manual launch test, and it adds no binary asset to the repo. The docs set is already inconsistent here (docs/Windows.md has no embedded image), so leaving it out breaks no convention the repo actually follows.</advantage>
      <drawback>The guide looks less like docs/Linux.md, and users get no visual check that they are running the right app. It is also as hard to discover as the other platform guides, because nothing links to it.</drawback>
    </alternative>
    <alternative id="Text-only plus README links">
      docs/macOS.md has no screenshot, and the root README.md gets a short platform-guides section that links docs/Linux.md, docs/Windows.md and docs/macOS.md.
      <advantage>The guide can be finished entirely within the milestone&apos;s dry-run scope, with no dependency on launching the app, and the macOS guide and the existing platform guides are all findable from the repo landing page.</advantage>
      <drawback>It still edits the stock mopro template README.md, which widens the milestone beyond macOS packaging. The guide also looks different from docs/Linux.md, which leads with a screenshot.</drawback>
    </alternative>
    <recommendation option="Text-only, no new links">A screenshot needs a launched Release build, which the goal defers to the manual test after this milestone, and the root README is the stock mopro template, so the guide ships complete within the dry-run scope and the image can be added once the launch test happens.</recommendation>
  </open-question>
  <open-question id="Prerequisite checks scope">
    <question>Which prerequisites should the script check before building, and how strict should each check be? Candidates: host is arm64 macOS, Xcode command-line tools, flutter, CocoaPods, cargo/rustup with the aarch64-apple-darwin target, mopro, hdiutil/codesign/shasum, a clean git working tree, and building on a release tag or branch. Which of these fail the build and which only warn?</question>
    <alternative id="Option A">
      Tool checks fail, git checks warn. Before building, the script fails with an install hint for each missing tool that has no workaround. That covers an arm64 macOS host (`uname -s`/`uname -m`), the Xcode command-line tools (`xcode-select -p`), `flutter`, CocoaPods `pod`, `cargo`/`rustup` with `aarch64-apple-darwin` in `rustup target list --installed`, and `hdiutil`/`codesign`/`shasum`. A dirty working tree or a HEAD that is not a release tag or `release/*` branch only prints a warning. The `mopro` check follows whatever the mopro-handling decision says: it fails only if `mopro build` is going to run.
      <advantage>Every toolchain gap shows up in the first seconds with a clear fix, not deep inside a long `mopro build`/`flutter build macos`/cargokit run. The script still works for this milestone&apos;s dry run, which happens on the feature branch `user/lukasz/macos_dist` with a tree that `mopro build` may itself dirty. It also matches the Goal&apos;s existing pattern of warning without stopping, as for the version mismatch.</advantage>
      <drawback>Nothing stops a maintainer from publishing a dmg built from uncommitted or untagged code. The warning is easy to scroll past, so the dmg on the GitHub release may not match any commit, and whether it is reproducible depends on the maintainer&apos;s discipline.</drawback>
    </alternative>
    <alternative id="Option B">
      Everything fails, with explicit overrides. Every candidate check fails the build, including the git checks: a dirty working tree fails and so does a HEAD that is not a release tag or `release/*` branch. Flags like `--allow-dirty` and `--allow-untagged` (or one `--dev` flag) turn the git failures into warnings. The clean-tree check runs before `mopro build`, so regenerated bindings are not counted against it.
      <advantage>By default, any artifact that could be released comes from a committed, tagged state, so the published SHA-256 can be traced to a specific commit. Skipping that guarantee takes a deliberate flag that anyone can see.</advantage>
      <drawback>The milestone&apos;s own dry run on a feature branch cannot pass without the override flags, and so will every day-to-day test build. This adds flag handling and documentation for a solo developer-machine workflow. If regenerated bindings are expected to be committed as part of the release, the clean-tree rule can also clash with `mopro build` dirtying the tree.</drawback>
    </alternative>
    <alternative id="Option C">
      Minimal presence checks. The script fails only if the host is not arm64 macOS or a command it calls directly is missing from `PATH` (`command -v` for `flutter`, `pod`, `cargo`, `hdiutil`, `codesign` and `shasum`, plus `mopro` if it runs). It does not check the Xcode tools setup or the installed Rust target, and it does not check git state. Deeper problems are left to `flutter build macos`, CocoaPods and cargokit to report.
      <advantage>It is the smallest and least brittle script. It does not try to reproduce checks the underlying tools already do, and it will not falsely fail on some other valid way of installing rustup/Xcode.</advantage>
      <drawback>Some problems only show up after minutes of building, as cryptic cargokit or Xcode errors, for example a missing `aarch64-apple-darwin` target or the command-line tools pointing to the wrong Xcode. The script also says nothing about dirty or untagged release builds.</drawback>
    </alternative>
    <depends-on question="mopro build handling" option="Always run, fail if missing"/>
    <recommendation option="Option A">Failing fast on missing tools, including mopro since it always runs, surfaces toolchain gaps in seconds with a fix, while warning on git state lets this milestone&apos;s dry run pass on the feature branch and matches the goal&apos;s warn-without-stopping pattern.</recommendation>
  </open-question>
  <open-question id="Verification checks and outcomes">
    <question>Which checks should the script run after building, and what counts as success? For example: codesign --verify --deep --strict on the app, checking the entitlements are present, hdiutil verify on the dmg, a test mount, and spctl --assess. spctl is expected to reject an ad-hoc-signed app, so should it be reported for information only?</question>
    <alternative id="Minimal hard gate">
      The script fails the build only if `codesign --verify --deep --strict` fails on the built `Zero Knowledge Migration App.app` or `hdiutil verify` fails on the finished dmg. It does not check entitlements and does not mount the dmg. It leaves `spctl --assess` out, or at most prints its expected rejection with an &quot;informational&quot; label and never lets it change the exit status.
      <advantage>Only two checks can fail, and both are reliable system-tool results. The script stays short, and the dry run can pass without any environment-specific test mounts or output that is hard to read.</advantage>
      <drawback>A signature can be valid and still be missing the `app-sandbox` and `network.client` entitlements, since `codesign --force --deep -s -` without `--entitlements` drops them silently. This gate would not catch that, or a dmg missing the app or the `/Applications` shortcut. Those problems would only show up in the later manual launch test. How much the entitlement gap matters depends on how the entitlements question is settled.</drawback>
    </alternative>
    <alternative id="Layered gate, spctl informational">
      After building, the script runs several checks, and any failure stops it with a non-zero exit. The checks are: - `codesign --verify --deep --strict --verbose=2` on the app. - `codesign -d --entitlements -` compared with the expected set in `flutter/macos/Runner/Release.entitlements`. - `hdiutil verify` on the dmg. - A read-only, no-browse test mount (`hdiutil attach -readonly -nobrowse`). It confirms the `.app` and the `/Applications` symlink are present, re-runs `codesign --verify` on the mounted copy, and then detaches in a trap. `spctl --assess --type execute` is run last. Its expected rejection of an ad-hoc signature is printed as information and never fails the run.
      <advantage>It catches each way the pipeline is likely to break: lost entitlements after re-signing, a dmg that is corrupt or missing its contents, and a signature damaged by the copy into the image. The dry-run success criterion then covers the whole build → sign → dmg chain, not just the two tools&apos; exit codes.</advantage>
      <drawback>It adds more code that has to be right, including a mount/detach trap and entitlement comparison against plist or XML output. The entitlement comparison assumes a known expected set, so if the entitlements question settles on something other than the current `Release.entitlements`, this check has to be changed to match. A leftover mount from an interrupted run can also make later runs fail.</drawback>
    </alternative>
    <alternative id="Layered gate plus bundle assertions">
      This runs every check in the layered gate above. It also fails on these bundle-content checks: - `lipo -archs` on the main executable and on the bundled frameworks must report `arm64`, and this milestone ships an arm64-only build. - `Info.plist` `CFBundleIdentifier` must be `com.zilliqa.zkpRecoveryApp`. - `Info.plist` `CFBundleShortVersionString` must match the version used in the dmg file name. `spctl` is still information only.
      <advantage>It guarantees the shipped artifact is what the file name and docs say it is: the right architecture, bundle identity and version. This matters because the `Info.plist` version comes from `flutter/pubspec.yaml` (0.5.0) and the other version files say 0.5.1, so a dmg name and a bundle version that disagree could otherwise be released without anyone noticing.</advantage>
      <drawback>It is the most code and the most likely to fail on things that are expected. The version check would fail today because of the 0.5.0/0.5.1 mismatch, which the milestone explicitly only warns about and leaves unfixed. So this check has to be downgraded to a warning, or it depends on how the version-source question is settled. It also partly overlaps with the prerequisite and version-warning steps.</drawback>
    </alternative>
    <recommendation option="Layered gate, spctl informational">The entitlement comparison against Release.entitlements and the test mount catch exactly the failures re-signing and dmg creation can introduce, so the dry run&apos;s success covers the whole build, sign and dmg chain, while the bundle assertions add overlap with the version warning for little extra safety.</recommendation>
  </open-question>
  <open-question id="Maintainer doc location">
    <question>Where should the maintainer instructions for the build script live: a --help/usage block in the script, a &quot;Build from source / Release&quot; section in docs/macOS.md like docs/Linux.md has, a separate maintainer doc, or a combination?</question>
    <alternative id="Help plus macOS.md section">
      The script gets a `--help`/usage block listing its flags and prerequisites, and `docs/macOS.md` ends with an &quot;(Alternative): Build from Source / Release&quot; section, matching `docs/Linux.md`. That section walks through clone, installing the toolchain (including `cargo install mopro-cli`, which is not on this machine yet), running the script, and publishing the dmg and its SHA-256.
      <advantage>It follows the precedent already set by `docs/Linux.md` (download → verify → run → &quot;Build from Source&quot;). A maintainer at the terminal and a reader browsing `docs/` each find instructions where they look, and it needs no new file beyond the user guide the milestone already requires.</advantage>
      <drawback>The same instructions live in two places: the script&apos;s usage text and the doc section. They can drift apart whenever flags or prerequisites change. The section also mixes maintainer release steps (publishing the checksum, and tagging too if the tag becomes a requirement) into a guide meant for end users, who mostly only need to download, check the checksum and get past Gatekeeper.</drawback>
    </alternative>
    <alternative id="Script help only">
      All maintainer guidance lives in the script itself, in a header comment plus a `--help`/usage block covering prerequisites, flags, outputs and the release checklist. `docs/macOS.md` stays a pure end-user guide, with at most a one-line pointer to the script.
      <advantage>There is one source of truth, and it sits next to the code it describes, so it is the easiest to keep accurate as the script changes. The user guide stays short and focused on download, checksum, install and Gatekeeper.</advantage>
      <drawback>It breaks the pattern of `docs/Linux.md`, which gives readers a &quot;Build from Source&quot; path in the docs. Anyone browsing `docs/` or GitHub can&apos;t find how releases are made unless they already know the script exists. Steps that happen outside the script, such as attaching the dmg and its checksum to the GitHub release, fit awkwardly in usage text.</drawback>
    </alternative>
    <alternative id="macOS.md section only">
      The full maintainer instructions go in a &quot;Build from Source / Release&quot; section of `docs/macOS.md`, as in `docs/Linux.md`. The script prints only a minimal usage line, or an error that points to the doc, and has no detailed help of its own.
      <advantage>It is the closest match to the existing Linux guide, keeps all prose documentation in the `docs/` tree where the platform guides already live, and keeps the script lean with nothing to duplicate.</advantage>
      <drawback>A maintainer running the script has no built-in reference for its flags or prerequisites and has to switch to the doc. The user-facing guide also carries release-maintainer content. This matters more if the script ends up with several flags, for example for `mopro build` handling or a clean build.</drawback>
    </alternative>
    <alternative id="Separate maintainer doc">
      A dedicated maintainer document is added alongside a `--help` block in the script. It could be a `docs/macOS-release.md`, a general `RELEASING.md`, or a README next to the script. `docs/macOS.md` stays user-facing and links to the maintainer doc, if it mentions building at all.
      <advantage>It cleanly separates the two audiences. End users get a short, focused guide, and maintainers get room for the full release procedure: toolchain setup, the version-consistency warning, verification output, and publishing the dmg and checksum. A general `RELEASING.md` could also later cover Linux and Windows, which have no build scripts today.</advantage>
      <drawback>It adds another file for a small repo that has no maintainer-doc convention yet. It also still duplicates part of the script&apos;s usage text. How natural a README next to the script is depends on where the script ends up living, which is a separate open question.</drawback>
    </alternative>
    <depends-on question="mopro build handling" option="Always run, fail if missing"/>
    <depends-on question="Clean build policy" option="Targeted clean only"/>
    <recommendation option="Help plus macOS.md section">It follows the docs/Linux.md precedent the goal asks for, gives maintainers help at the terminal and a browsable release section in the guide, and with a script that has almost no flags the duplication between the two stays small.</recommendation>
  </open-question>
</open-questions>
